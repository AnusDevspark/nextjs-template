# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files. `.env.example` is intentionally NOT ignored — it is the contract.
# NEXT_PUBLIC_API_URL is required with no default, so a clone without it fails to
# boot with a Zod error and no hint about what to set.
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# testing
/coverage
/playwright-report
/test-results
/blob-report
/.playwright

# generated API types (regenerate with `npm run generate:api`)
/src/generated/*.ts
